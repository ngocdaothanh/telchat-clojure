This is only an exercise in Clojure:
* Create a chat server using JBoss Netty
* Clients are telnet programs

Usage:
* To compile: make, you must have Maven2
* To run: make start, then telnet localhost 3000
